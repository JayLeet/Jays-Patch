# Feature Map

Use this map to find the owner files for a Jay's Patch feature before editing.
For exact call links, use `generated/function-index.md`.

| Feature | Ownership | Primary sources | Notes |
| --- | --- | --- | --- |
| Main tick chain | Runs all Jay's Patch recurring systems. | `botc_patch:tick` | Calls reset, queue, vote, setup-room input, setup-wall clicks, Storyteller tools, grimoire, hand, winner, music, item-maintenance, and the setup-only Jay's Patch toggle. The toggle flags can disable Jay-held item systems without disabling passive cleanup or Reveal Grimoire. |
| Hosted startup YAWP repair | Schedules Sybillian/YAWP startup repair after Jay's Patch loads on hosted servers that do not run `BOTC.exe`. | `botc_patch:load`, `startup/yawp_init`, `startup/yawp_flags`, `startup/yawp_reset`, `startup/yawp_regions`, `launcher/exe/BotcLauncher.cs` | `load` resets `yawp_startup_done` to `0` and schedules `startup/yawp_init` after 20 seconds with `replace`. The delayed function runs Jay-owned macro-wrapped YAWP commands copied from Sybillian's init intent, because Sybillian's direct `yawp ...` functions can fail to load before YAWP config is ready. `BOTC.exe` calls the same wrapper during Final Sync and sets the flag to `1`, preventing duplicate YAWP runs on the local launcher path. This is intentionally separate from gameplay tick logic. |
| First-install server-properties notice | Shows joining users where to find the required server-properties note until the world-level notice is disabled. | `install_notice/*`, `botc_patch:load`, `botc_patch:tick`, `Jays-Patch/datapack/jays-patch-required-server-properties.txt`, `Jays-Patch/server-config/jays-patch-required-server-properties.txt` | The notice is player-scoped with `tellraw @s`, appears once per login session by comparing `botc_leave_game` to `botc_install_seen_leave`, and can be disabled through the clickable `/trigger botc_install_notice_disable set 1` path. Clickable text copies the datapack file path or runs the trigger; it does not try to open a server-local file on clients. |
| Raise/lower hand | Nomination-only item, duplicate cleanup, dropped item cleanup, and seat lamps. | `hand/*`, `Jays-Patch/item-fallbacks.json`, `tools/generate-item-fallbacks.ps1` | Slot 4 is preferred; fallback functions protect Sybillian tools and are generated from the shared fallback table. |
| Grimoire reveal | Reveal confirmation, one-time intro sweep, pre-reveal character editor, one-by-one role reveal, cleanup, and spotlight visuals. | `grim/*`, `grim/editor/*`, `grim/reveal/*`, `grim/sweep/*`, `cmd/character*`, `Jays-Patch/grim-sweep.json`, `Jays-Patch/item-fallbacks.json`, `tools/generate-grim-sweep.ps1`, `tools/generate-grim-dialog.ps1`, `tools/generate-grim-character-editor.ps1`, `tools/generate-character-sync.ps1` | During an active game, the editor caches server-authoritative seat role/alignment state and reads literal player names from Sybillian's game-start `ct:players` snapshot. `Change Characters` exists only in the pre-reveal confirmation and opens generated count-based player and current-script role dialogs. Role buttons stay in Sybillian category order and use blue Townsfolk, light-blue Outsider, orange Minion, and red Demon labels; dark bold Good/Evil controls occupy their own first row and override reveal alignment only. Character edits update Jay's cached reveal state and directly send `fmvariable set p<seat>_role` to the acting Storyteller so Sybillian's FancyMenu grimoire refreshes without changing seated-player role scores or gameplay tags. Confirming Reveal Grimoire refreshes non-overridden live roles, captures the final seat snapshot, applies cached overrides, and sets `grim_editor_reveal_started` before the cinematic sweep. Sybillian's standard Storyteller `ct-grimoire` picker remains client-local FancyMenu state and cannot be observed server-side, so this explicit editor is the reliable unchanged-client path. The active reveal dialog remains the bounded one-page count design and renders snapshot-backed `Player (Role)` labels in Townsfolk/Outsider/Minion/Demon category colors; Good Wins and Evil Wins occupy the first row, and terminal reveal/winner actions close instead of leaving a waiting screen. Do not restore grouped ranges or the rejected 32,768-file mask design. Role reveals use role-score mapping and paper custom model data. |
| Winner reveal | Good/evil win titles, suspense jingle, temporary head blocks, timer, and cleanup. | `winner/*` | Trigger through `/botc winner good\|evil` or Storyteller menu/dialog actions, not command blocks. The "The winning team is..." pause uses a neutral timed sound ladder before the final Good/Evil stinger. |
| Night music | Detects phase changes, maintains the Night Music selector item, and plays low-volume music on the client music mixer. | `music/*`, `Jays-Patch/item-fallbacks.json` | Automatic night music uses real vanilla `minecraft:music.*` events; manual selector choices keep extra disc-style tracks available. Variant tables and selector fallback functions are generated-style command tables. |
| Storyteller commands | Adds/removes Storyteller state by wrapping Sybillian behavior. | `storyteller/*` | Normal players and spectators become Storyteller through approved queue/admin paths. Direct Storyteller add/remove is not a public `/botc` surface; players and spectators must not receive Storyteller-only permissions unless they become Storyteller through one of those paths. |
| Storyteller live-game tools | Fixed-slot live-game Storyteller items, phase advance/reset wrappers, player action dialogs, teleports, timer dialog, and night invisibility. | `storyteller_tools/*`, `Jays-Patch/tool-items.json`, `tools/generate-tool-items.ps1`, `tools/lib/player-dialog-generator.ps1`, `tools/generate-storyteller-player-menu.ps1`, `tools/generate-storyteller-kill-menu.ps1`, `tools/generate-storyteller-revive-menu.ps1` | Active only during phase `1..` when `patch_items_enabled` is `1`. Passive safety behavior such as night invisibility, passage restore, and temporary-menu closure stays in `storyteller_tools/passive_tick` so it can still clean up correctly if Jay-held items are disabled. The fixed live Storyteller hotbar row, live item cleanup/repair checks, and the post-execution follow-up row are generated from `tool-items.json`; update the registry and rerun `tools/generate-tool-items.ps1` instead of hand-editing generated rows. Reveal Grimoire stays in visual slot 7 (`hotbar.6`) during normal hotbar states. Teleport, Kill, and Revive open generated vanilla dialogs with current `Player (Role)` labels in role category colors. Kill compacts only alive seated players; Revive compacts only dead seated players. Their guarded `/botc` seat actions close the dialog before calling Sybillian `ct:kill/die` or `ct:kill/revive`. The bounded shared generator emits 16 count variants rather than mask combinations, so sparse eligibility remains safe at 15 seats. Storyteller's Passage, timer, phase-specific teleports, and night visibility retain their established behavior. |
| Nomination Storyteller tools | Nomination-only dialog for selecting a nominee, followed by guarded hotbar controls for voting and execution marking. | `storyteller_tools/nomination_menu/*`, `storyteller_tools/post_execution/*`, `tools/lib/player-dialog-generator.ps1`, `tools/generate-storyteller-nomination-menu.ps1`, `Jays-Patch/tool-items.json` | Active only during phase `3`. Nominate opens a bounded vanilla dialog containing all active seated players as `Player (Role)` in role category colors. Selecting a player immediately calls Sybillian `ct:admin/nomination` and opens Back plus Start Vote. Back fully rescinds an unfinished nomination or active vote, including schedules, voting items/tags, marker visibility, clock hand, and bossbar, while preserving prior completed-vote thresholds and execution marks. Start Vote remains available; pressing it again cancels only transient vote machinery, rebuilds the selected nomination through `ct:admin/nomination`, and starts a clean replacement vote instead of stacking schedules. Mark appears only after Sybillian applies `last_nom` to the selected player at vote completion. It toggles in place between Mark and Clear Mark; Back exits the completed-vote controls. Pyre and Execute remain on the main nomination hotbar. Jay's Execute stores the marked player before Sybillian clears `marked_for_execution`, then shows the post-execution row; post-execution Kill targets only that stored player. |
| Storyteller queue | Lets players and spectators queue for the next Storyteller slot, promotes the longest-waiting online queued user during setup when no active Storyteller is online, and cleans stale Storyteller state after normal setup handoff/rejoin. | `queue/*`, `storyteller/*`, `reset/*`, `melius-commands/commands/botc.json` | Queue order uses persistent scores plus player tags, so queued state survives game resets and server restarts. Live players can join the queue during a game, but they are not promoted during a live game. If no Storyteller is online during a live game, the oldest queued online spectator can be added as an emergency extra Storyteller without incrementing the active Storyteller generation, so the original Storyteller is not replaced if they return. The active Storyteller generation marker prevents old Storyteller permissions from returning after normal setup handoff. |
| Setup queue/tools | Setup-phase hotbar items for resetting setup while keeping Storyteller state, leaving Storyteller state, joining/leaving queue, checking queue status, and toggling Jay's Patch item behavior. | `setup_tools/*`, `queue/*`, `setup_room/*`, `patch_toggle/*`, `botc_patch:load`, `Jays-Patch/tool-items.json`, `tools/generate-tool-items.ps1` | During phase `0`, Storytellers get Reset Game, Become a Player, and Toggle Jay's Patch unless they are actively using the setup room. Toggle Jay's Patch sits in visual slot 6 (`hotbar.5`) and cycles instantly on right-click: everything enabled -> Sybillian setup bag -> Jay-held items disabled -> everything enabled. The Toggle item renders as a lime candle when everything is enabled, an orange candle when only Sybillian's setup bag is active, and a red candle when Jay-held items are disabled. Non-queued players get Become the Storyteller. Queued players get Queue Status and Leave Queue. Toggle Jay's Patch is deliberately maintained outside the generated setup-tool row so it still works when Jay-held items are disabled. Its three setup-only actions are: use Sybillian's Setup Bag by setting `patch_setup_bag_enabled` to `0`; disable Jay-held items by setting both `patch_items_enabled` and `patch_setup_bag_enabled` to `0` while keeping Toggle Jay's Patch and Reveal Grimoire available; and re-enable everything by setting both flags to `1`. `botc_patch:load` owns a versioned one-time migration that forces both flags to their documented fully enabled defaults exactly once, even if a distributed world template persisted an older state; subsequent user toggles survive reloads. The fixed setup-tool hotbar, cleanup files, and setup item-check repair file are generated from `tool-items.json`; update the registry and rerun `tools/generate-tool-items.ps1` instead of hand-editing those generated files. Setup Reset calls Sybillian reset behavior and Jay cleanup while preserving the caller's Storyteller tag; Become a Player is the only setup item that removes Storyteller state. |
| Votekick | Public majority-vote kick flow with one active target, cooldowns, timeout cleanup, and owner immunity. | `vote/*`, `melius-commands/commands/botc.json`, `launcher/exe/BotcLauncher.cs` | Majority is strict `floor(eligible / 2) + 1`, excluding the target. Votes expire after two minutes. `function-permission-level=3` is owned by the launcher so the datapack can run `kick` only after the vote succeeds. If the target is a Storyteller, Jay's Patch removes their Storyteller state before kicking them so those powers do not persist when they rejoin. |
| Owner immunity | Configured owner list used for queue bypass and votekick target immunity. | `Jays-Patch/config/owners.txt`, `config/owners`, `tools/generate-owner-config.ps1` | Edit the text config, regenerate, deploy, and reload. Static owners are applied to `botc_owner_static` each tick and merged into `botc_owner`; owner add/remove is no longer exposed through public `/botc` commands. |
| Setup commands | Role on/off, clear, built-in preset, import, and apply bridge into Sybillian setup flow. | `setup/*`, `tools/generate-setup-role-clear.ps1` | `/setupbag` is the setup-game command surface. Setup mutation is phase-guarded to pre-game phase `0` and Storyteller-guarded before server-authority execution. High-impact preset, import, clear, and apply entrypoints route through `setup/bridge/*`, which uses `botc_setup_bridge_cd` as a short per-Storyteller debounce; role toggles stay unthrottled so normal checkbox-style setup remains responsive. Built-in presets set the same `role_list` scores as the checkbox buttons, copy preset roles onto the Script item's `script.characters` data for FancyMenu display, then call Sybillian's `set_from_menu`. Custom `/setupbag import <script-json>` calls Sybillian's full import sequence, then clears selected Jay setup-wall roles, syncs the empty selection to Sybillian setup state, and redraws the setup wall from `ct:script in_characters`; treat it as blocked if the 65,536-command limit returns. The setup-bag preset buttons close the GUI after selection so FancyMenu rereads the refreshed Script item when reopened. Role-list cleanup is generated from the known role table instead of resetting every scoreboard holder. |
| Setup room and role wall | Server-side visual setup room, replacement setup bag, base-script selector items, post-mode setup action items, role icons, role-name labels, interaction hitboxes, and Sybillian `role_list` scores. | `setup_room/*`, `setup_wall/*`, `Jays-Patch/base-scripts.json`, `Jays-Patch/tool-items.json`, `tools/lib/sybillian-role-catalog.ps1`, `tools/generate-tool-items.ps1`, `tools/generate-setup-wall.ps1` | Base-script membership is owned once in `base-scripts.json`; IDs, display names, categories, and alignment metadata are parsed from Sybillian's installed role tables. The replacement setup bag uses Jay-owned `setup_wall_bag` model data while visually reusing Sybillian's black setup-bag model, and is only repaired when both `patch_items_enabled` and `patch_setup_bag_enabled` are `1`. If Jay's setup bag is disabled, setup-room state is cleared and Sybillian's `ct_bag`, Grimoire, and Script are repaired only when missing or moved. Storyteller clicks are accepted only during phase `0`. Opening the Setup Bag teleports the Storyteller to the room, shows a two-line colored chat guide, and clears the wall instead of assuming a default script. Setup-room bag cleanup/repair plus control/action hotbar rows are generated from `tool-items.json`; update the registry and rerun `tools/generate-tool-items.ps1` instead of hand-editing `item_checks.mcfunction`, `give_controls.mcfunction`, or `give_action_controls.mcfunction`. Script selector items clear selected roles, sync Sybillian script storage and the Storyteller Script item, redraw the wall for TB/SNV/BMR, and leave every role disabled until the Storyteller clicks individual role icons. Custom imports redraw the same wall from Sybillian `ct:script in_characters`, so custom scripts are server-side display data rather than client UI data. The Custom Script item opens a typed dialog that submits pasted JSON through guarded `/setupbag import`. The setup-wall tick only coordinates clicked interactions and throttled particle refresh; generated click/particle dispatchers cover the full known Sybillian role list without running the full scan every tick. TB, SNV, and BMR sit in visual slots 2-4 (`hotbar.1..3`), and Custom Script sits in visual slot 5 (`hotbar.4`). Role names are generated as small text displays above the icons, blue for town/outsider and red for minion/demon. After a mode is selected, visual slots 2-4 (`hotbar.1..3`) become Clear setup, Use this bag, and Start, while visual slots 5-6 are cleared. Clear setup resets all known role scores and rebuilds Sybillian `ct:roles`; Use this bag applies the current active roles through `ct:admin/setup/set_from_menu`; Start silently applies the wall, clears wall highlight visuals before start, clears stale previous-game player role state through Sybillian `ct:util/reset_player`, runs Sybillian's start setup, teleports the Storyteller to the Storyteller den, plays the bell cue after that teleport, and then runs the normal `ct:start_game/roles/youare` announcement flow. A one-shot setup-wall cleanup also runs when phase leaves setup, so selected-role visuals are cleared even if the game starts through another command path. Enabled roles use lifted/scaled bright icons and dense white wall-hugging particle rings during setup phase, avoiding persistent glow outlines or display panes. The Back item returns the Storyteller to world spawn, restores their previous gamemode, and clears temporary setup-room controls. `setup_room/teleport_private` owns only the setup-room coordinate. The wall toggles Sybillian `role_list` scores through `ct:admin/setup/set_from_menu`. |
| Reset | Calls Sybillian reset behavior and returns users to normal player state without restarting the server. | `cmd/reset_game`, `reset/*` | Uses a reset-generation marker so players who were offline during reset are cleaned on rejoin. Reset also clears interrupted Sybillian nomination/vote state, including scheduled vote loops, vote bossbar, vote items, nomination tags, vote scoreboards, vote markers, and the clock hand, so an abandoned nomination phase cannot trap the next Storyteller. During a live game, online players whose `game_id` does not match `active_game` have stale role/team tags stripped without changing their spectator/location state. `Jays-Patch/world-template` stays the shareable/manual recovery source, not the normal reset path. |
| Command overlays | Public and Melius broker command surfaces. | `melius-commands/commands/*.json` | `/st`, `/setupbag`, `/settings`, `/tpchurch`, `/tpallhome`, `/request_chat`, and `/character` are Sybillian-style broker surfaces for FancyMenu and Storyteller actions. `/botc` remains for Jay-owned custom features and manual compatibility fallbacks. |
| Resource pack | Custom hand/reveal textures and role icon model wiring. | `resourcepack/*`, `Jays-Patch/role-icons.json`, `Jays-Patch/tool-items.json`, `tools/generate-role-icons.ps1`, `tools/generate-tool-items.ps1`, `tools/tests/test-resourcepack-mappings.ps1`, `tools/tests/test-tool-item-registry.ps1` | Role art is generated from Sybillian textures into Jay-owned item textures because direct `ct:role/...` item-model references rendered as missing textures. Role paper mappings, per-role model files, and copied role PNGs are generated from the shared role list. `tool-items.json` is the registry for Jay-owned right-click tool labels, slots, phases, model-data ownership, and generated fixed-row cleanup/repair ownership. The registry and resource-pack mapping tests catch drift between datapack item strings, item override JSON, and model files. |
| Standalone launcher | Server startup, approved stop-time backups, deploy, resource-pack config, server-list branding, server-property ownership, and console UI. | `BOTC.exe`, `launcher/exe/BotcLauncher.cs`, `launcher/exe/BotcLauncher.Models.cs`, `launcher/exe/BotcLauncher.Process.cs`, `launcher/branding.txt`, `launcher/compose.yml`, `launcher/local-settings.example.properties`, `../data/server-icon.png`, `tools/build-botc-exe.ps1`, `tools/tests/test-startup-scripts.ps1`, `tools/tests/test-source-safety.ps1`, `tools/tests/test-source-ownership.ps1` | The launcher compiles all C# files under `launcher/exe`; data/config models and external-process/RCON execution are behavior-preserving extractions from the main startup hotspot. `BOTC.exe` is the user-facing standalone entrypoint; it starts Docker Desktop when installed but not running, starts the server when offline, and opens console-only mode when online without calling runtime PowerShell launcher scripts. Its help menu only lists launcher-owned console commands; Minecraft commands are typed directly and sent to the server without local autocomplete. The `stop` command asks before replacing `backups/standard`; the retired `backups/latest` and `promote-backup` flow should not return. Interactive windows use the fixed dashboard; redirected/non-interactive startup deliberately skips cursor rendering and emits linear status lines. Console `WARN` output is reserved for actionable problems; non-error operational notes should use `NOTE` or dashboard details. Run `tools/tests/test-startup-scripts.ps1` after launcher edits, or `tools/tests/test-source-safety.ps1` during source-only stabilization. It deploys into the Docker-mounted server data folder, reads visible branding from `launcher/branding.txt`, uses Minecraft's normal `../data/server-icon.png` as the single logo source, keeps `function-permission-level=3` and `spawn-protection=0` in `server.properties`, and `launcher/compose.yml` must exclude Jay-owned command/menu runtime folders from Modrinth overwrites. Final Sync remains the launcher-owned post-start deploy/reload path; hosted servers that do not run `BOTC.exe` use the tiny delayed datapack YAWP repair instead of Docker `RCON_CMDS_STARTUP`. The ownership and upstream-contract tests guard the Lego rule by keeping Jay-owned datapack behavior out of upstream `ct` namespaces and proving direct upstream calls still resolve. |
| Clean world template | Command-block-free world for sharing Jay's Patch and manual recovery. | `Jays-Patch/world-template` | Copied from cleaned `../data/world`; keep command blocks out of this template. |

## Script Selection Transaction

TB, SNV, BMR, and custom JSON imports all rebuild the complete Sybillian
`ct:script` state through `setup/import/commit*`. This includes role categories,
night order, reminders, `current_script`, normalized name/author metadata, and the
generated Script item. Role enablement remains separate, so choosing a setup-room
script still leaves every wall role disabled until the Storyteller selects it.

Opening or cancelling the Custom Script dialog does not mutate the current setup.
Validation failures restore every backed-up `ct:script` field and regenerate the
previous Script item before temporary import state is removed.

## Storyteller's Passage State

Passage snapshots one numeric per-player voice-zone ID when it opens. The shared
zone detector mirrors every Sybillian voice marker except Town Square and detects
the Storyteller Den directly, without waiting for Sybillian's `den` tag. Starting
inside a valid zone still requires leaving it before that same zone can close
Passage during the day. At night, Passage can only open inside a player house;
leaving that starting house immediately teleports the still-spectating
Storyteller to the den, clears stale `in_house`, and only then restores the saved
mode and hotbar. Phase changes, Storyteller-role loss, and item-disable cleanup
request a safe close through the same two-block body-space guard instead of
restoring a player inside a wall. If night ends while Passage is open, it closes
safely before `night_exit` restores the true pre-night gamemode.

