# Feature Map

Use this map to find the owner files for a Jay's Patch feature before editing.
For exact call links, use `generated/function-index.md`.

| Feature | Ownership | Primary sources | Notes |
| --- | --- | --- | --- |
| Main tick chain | Runs all Jay's Patch recurring systems. | `botc_patch:tick` | Calls reset, dynamic seat layout, nomination-marker compatibility, queue, vote, setup-room input, setup-wall clicks, Storyteller tools, grimoire, hand, Banshee, winner, music, item-maintenance, and the setup-only Jay's Patch toggle. The toggle flags can disable Jay-held item systems without disabling passive cleanup or the Storyteller Tools entrypoint. |
| Hosted startup YAWP repair | Schedules Sybillian/YAWP startup repair after Jay's Patch loads on hosted servers that do not run `BOTC.exe`. | `botc_patch:load`, `startup/yawp_init`, `startup/yawp_flags`, `startup/yawp_reset`, `startup/yawp_regions`, `launcher/exe/BotcLauncher.cs` | `load` resets `yawp_startup_done` to `0` and schedules `startup/yawp_init` after 20 seconds with `replace`. The delayed function runs Jay-owned macro-wrapped YAWP commands copied from Sybillian's init intent, because Sybillian's direct `yawp ...` functions can fail to load before YAWP config is ready. `BOTC.exe` calls the same wrapper during Final Sync and sets the flag to `1`, preventing duplicate YAWP runs on the local launcher path. This is intentionally separate from gameplay tick logic. |
| First-install server-properties notice | Shows joining users where to find the required server-properties note until the world-level notice is disabled. | `install_notice/*`, `botc_patch:load`, `botc_patch:tick`, `Jays-Patch/datapack/jays-patch-required-server-properties.txt`, `Jays-Patch/server-config/jays-patch-required-server-properties.txt` | The notice is player-scoped with `tellraw @s`, appears once per login session by comparing `botc_leave_game` to `botc_install_seen_leave`, and can be disabled through the clickable `/trigger botc_install_notice_disable set 1` path. Clickable text copies the datapack file path or runs the trigger; it does not try to open a server-local file on clients. |
| Raise/lower hand | Nomination-only item, duplicate cleanup, dropped item cleanup, and seat lamps. | `hand/*`, `Jays-Patch/item-fallbacks.json`, `tools/generate-item-fallbacks.ps1` | Slot 4 is preferred; fallback functions protect Sybillian tools and are generated from the shared fallback table. |
| Dynamic town-square seats | Rebuilds the existing 15-chair town square into an evenly distributed clockwise layout for the eligible pregame player count, then locks that layout for the game. | `seat_layout/*`, `nomination_markers/*`, `tools/generate-seat-layouts.ps1`, `tools/tests/test-seat-layouts.ps1`, `tools/tests/live/stress-seat-layouts-ingame.ps1` | Eligible players exclude Storytellers and spectators. Setup polling applies layouts `0..15` only when the eligible count changes; game start restores Sybillian's baseline for assignment, locks the selected layout, and repositions persistent score-identified vote markers without recreating them. Each active marker receives a normalized `transformation.left_rotation` quaternion aimed at the exact town-square center, matching Sybillian's item-display orientation convention, and each chair sign uses the canonical 15-seat hex color for its resolved player name. Players move to the locked seat layout only when the Storyteller explicitly uses Teleport Seats or its guarded command bridge; phase transitions never teleport them automatically. Nomination name compatibility reads Sybillian's game-start `ct:players` snapshot. A transition-only Jay wrapper corrects Sybillian 1.5.4's three proven dead-vote marker typos (`13->10`, `14->11`, `15->12`) while leaving `ct:` untouched and restoring the overwritten marker from its real player's vote state. The live stress harness covers counts 5-15, marker position and rotation, full clockhand cycles, alive/dead/ghost vote models, execution/death particle routes, hand lamps, and Reveal Grimoire sweep/reveal/cleanup; its exhaustive 15-seat pass checks every lamp and reveal marker individually. |
| Banshee adapter | Activates Sybillian's incomplete 1.5.4 Banshee scaffold and adds player-controlled x1/x2 voting. | `banshee/*`, `grim/awaken_banshee`, `grim/confirm`, `Jays-Patch/item-fallbacks.json`, `Jays-Patch/tool-items.json`, `tools/tests/test-banshee.ps1` | The Storyteller manually confirms a Demon-caused Banshee death through the contextual `Awaken Banshee` button because the server cannot infer the killer. Activation reuses `ct:cmd/banshee/announce` and upstream `active_banshee`. During phase `3`, the active Banshee keeps access to dead votes and receives a visual-slot-6 toggle, defaulting to x1. x2 stages exactly one bonus in `total vote` while a vote is active; Sybillian then adds the normal YES vote and evaluates the result. The upstream contract test proves this ordering. Cleanup uses a Jay-owned custom-data marker so role-icon items are not removed accidentally. Sybillian 1.5.4 has no authoritative nominator tracking, so the Banshee's second nomination remains Storyteller-managed rather than pretending to enforce it. |
| Grimoire reveal | Reveal confirmation, one-time intro sweep, pre-reveal character editor, one-by-one role reveal, cleanup, spotlight visuals, contextual role announcements, and accidental-start rollback. | `grim/*`, `grim/editor/*`, `grim/alhadikhia/*`, `grim/reveal/*`, `grim/sweep/*`, `Jays-Patch/grim-sweep.json`, `Jays-Patch/item-fallbacks.json`, `tools/generate-grim-confirm.ps1`, `tools/generate-grim-alhadikhia.ps1`, `tools/generate-grim-sweep.ps1`, `tools/generate-grim-dialog.ps1`, `tools/generate-grim-character-editor.ps1` | During an active game, the editor caches server-authoritative seat role/alignment state and reads literal player names from Sybillian's game-start `ct:players` snapshot. `Change Characters` exists only in the pre-reveal confirmation and opens generated count-based player and current-script role dialogs. Contextual controls are generated from an option mask: `Announce Fearmonger` appears for role `108`, `Awaken Banshee` for an eligible dead role `55`, and `Announce Al-Hadikhia Target` for role `128`. The Al-Hadikhia picker reuses the trusted `Player (Role)` labels and passes the chosen game-start player name into `ct:cmd/alhadikhia/announce`; this is Sybillian's polished public presentation only and does not implement Al-Hadikhia gameplay state. Role buttons stay in Sybillian category order and use blue Townsfolk, light-blue Outsider, orange Minion, and red Demon labels. The current role is repeated prominently in the dialog title and in a bold, underlined, alignment-colored status. Character selection changes only the role and preserves the seat's remembered alignment; the dark bold Set Good/Evil controls occupy their own first row and are the only role-editor actions that override reveal alignment. Character edits update Jay's cached reveal state and directly send `fmvariable set p<seat>_role` to the acting Storyteller so Sybillian's FancyMenu grimoire refreshes without changing seated-player role scores or gameplay tags. Shared `Player (Role)` labels use a white player name for readability and derive the `(Role)` suffix from effective alignment: blue for Good and red for Evil. This contract covers Change Characters, teleport, kill, revive, nomination, Al-Hadikhia, and the final reveal menu, including character/alignment mismatches. Confirming Reveal Grimoire refreshes non-overridden live roles, captures the final seat snapshot, applies cached overrides, and sets `grim_editor_reveal_started` before the cinematic sweep. Before the first role or winner is shown, the Storyteller may use the explicit Cancel Reveal button to confirm a rollback that removes reveal visuals and restores the captured phase, time, daylight cycle, and vote-marker visibility without clearing cached edits. Esc invokes a close-only route and never rescinds. Dialog mode repairs a held Reset Game item into visual slot 1 while reveal mode is active. Sybillian's standard Storyteller `ct-grimoire` picker remains client-local FancyMenu state and cannot be observed server-side, so this explicit editor is the reliable unchanged-client path. The active reveal dialog remains a bounded one-page count design, and revealed seats are omitted when the Storyteller reopens the menu. Good Wins and Evil Wins occupy the first row, and terminal reveal/winner actions close instead of leaving a waiting screen. Do not restore grouped ranges or the rejected 32,768-file mask design. Role reveals use role-score mapping and paper custom model data. |
| Winner reveal | Good/evil win titles, suspense jingle, temporary head blocks, five unique team-colored victory rockets per winner, timer, and cleanup. | `winner/*`, `setup/prepare_new_game_inventory`, `cmd/start` | Trigger through `/botc winner good\|evil` or Storyteller menu/dialog actions, not command blocks. The "The winning team is..." pause uses a neutral timed sound ladder before the final Good/Evil stinger. Diamond-block and piglin-head winner items carry Binding Curse and a Jay-owned marker, are repaired only when the protected head is missing, and can be removed through winner cleanup/reset paths. Each winning player receives exactly five marked rockets spanning all five vanilla explosion shapes with Good blue/cyan/white or Evil red/orange/crimson gradients. Reward generations prevent duplicate grants in one game. Normal reset and head cleanup deliberately preserve these rockets; the next supported game start clears carried and dropped marked rockets, and the generation check catches offline winners when they rejoin. Winners who were offline when the head timer expired are also cleaned when they rejoin. |
| Night music | Detects phase changes, maintains the Night Music selector item, and plays low-volume music on the client music mixer after players opt in. | `music/*`, `Jays-Patch/music-tracks.json`, `Jays-Patch/item-fallbacks.json`, `tools/generate-music.ps1` | Every night advances a generation counter and defaults each eligible player to music off exactly once for that generation; this also catches players who join, receive a seat, or leave spectator state after night has started without muting someone again after they opt in. Players turn music on themselves through the selector. The generated four-column selector exposes all 21 Minecraft 1.21.10 jukebox discs plus six retained ambient tracks. Every disc uses its vanilla disc texture; ambient choices use a matching block/item texture. Existing trigger IDs remain stable, trigger `22` remains Toggle Pitch, and random selection covers the contiguous 27-track pick range. Disc variants retain low randomized volume/pitch while the existing native ambient settings remain unchanged. |
| Storyteller commands | Adds/removes Storyteller state by wrapping Sybillian behavior. | `queue/*`, `reset/*`, `melius-commands/commands/st.json` | Normal players and spectators become Storyteller through approved queue/admin paths. Direct Jay-owned Storyteller add/remove helper functions are retired; players and spectators must not receive Storyteller-only permissions unless they become Storyteller through an approved path. |
| Storyteller live-game tools | Fixed-slot live-game Storyteller items, a phase-aware dialog-first dashboard, phase advance/reset wrappers, player action dialogs, teleports, timer dialog, and night invisibility. | `storyteller_tools/*`, `Jays-Patch/tool-items.json`, `tools/generate-tool-items.ps1`, `tools/lib/player-dialog-generator.ps1`, `tools/lib/seat-colors.ps1`, `tools/generate-storyteller-player-menu.ps1`, `tools/generate-storyteller-kill-menu.ps1`, `tools/generate-storyteller-revive-menu.ps1`, `tools/generate-teleport-evil-dialog.ps1` | Active only during phase `1..` when `patch_items_enabled` is `1`. `patch_dialog_mode=0` preserves the established item-first hotbar; `patch_dialog_mode=1` moves phase-level controls into `storyteller_tools/dashboard/*`, whose trigger route rechecks Storyteller authority, mode, and phase before calling the same guarded wrappers and submenus. Storyteller Tools, Storyteller's Passage, setup-room controls, and temporary nomination Back/Start Vote/Mark controls remain held items in both Jay modes. Storyteller's Passage uses visual slot 3 in daytime item mode and visual slot 6 in daytime dialog mode or night-house state. Dialog mode also keeps Reset Game in visual slot 1 only while Reveal Grimoire is active, preventing a non-op Storyteller from losing the reset path. Passive safety behavior such as night invisibility, passage restore, and temporary-menu closure stays in `storyteller_tools/passive_tick` so it can still clean up correctly if Jay-held items are disabled. The fixed live Storyteller hotbar row, mode-aware cleanup/repair checks, and post-execution follow-up row are generated from `tool-items.json`; update the registry and rerun `tools/generate-tool-items.ps1` instead of hand-editing generated rows. Storyteller Tools stays in visual slot 7 (`hotbar.6`) during active normal states and opens either the current phase dashboard or the active reveal controls. Teleport, Kill, and Revive retain their generated player dialogs and all existing submenus. The Evil Team control retains its Demon Only, Minions Only, or Demon + Minions dialog with current role icons. Teleport Home deliberately mirrors Sybillian's generic `tag=!in_house` behavior. |
| Nomination Storyteller tools | Nomination-only dialog for selecting a nominee, followed by guarded hotbar controls for voting and execution marking. | `storyteller_tools/nomination_menu/*`, `storyteller_tools/post_execution/*`, `tools/lib/player-dialog-generator.ps1`, `tools/lib/seat-colors.ps1`, `tools/generate-storyteller-nomination-menu.ps1`, `Jays-Patch/tool-items.json` | Active only during phase `3`. Nominate opens a bounded vanilla dialog containing all active seated players; each player name uses white and each `(Role)` suffix uses effective Good/Evil alignment color. Selecting a player immediately calls Sybillian `ct:admin/nomination` and opens Back plus Start Vote. Back fully rescinds an unfinished nomination or active vote, including schedules, voting items/tags, marker visibility, clock hand, and bossbar, while preserving prior completed-vote thresholds and execution marks. Start Vote remains available; pressing it again cancels only transient vote machinery, rebuilds the selected nomination through `ct:admin/nomination`, and starts a clean replacement vote instead of stacking schedules. Mark appears only after Sybillian applies `last_nom` to the selected player at vote completion. It toggles in place between Mark and Clear Mark; Back exits the completed-vote controls. Pyre and Execute remain on the main nomination hotbar. Jay's Execute stores the marked player before Sybillian clears `marked_for_execution`, then shows the generated post-execution row. Post-execution Kill targets only that stored player. The Boomdandy tool appears in visual slot 3 only while a seated non-spectator has Sybillian role `107`, and its guarded wrapper calls `ct:loop/boomdandy/start`. |
| Storyteller queue | Lets players and spectators queue for the next Storyteller slot, promotes the longest-waiting online queued user during setup when no active Storyteller is online, and cleans stale Storyteller state after normal setup handoff/rejoin. | `queue/*`, `storyteller/*`, `reset/*`, `melius-commands/commands/botc.json` | Queue order uses persistent scores plus player tags, so queued state survives game resets and server restarts. Live players can join the queue during a game, but they are not promoted during a live game. If no Storyteller is online during a live game, the oldest queued online spectator can be added as an emergency extra Storyteller without incrementing the active Storyteller generation, so the original Storyteller is not replaced if they return. The active Storyteller generation marker prevents old Storyteller permissions from returning after normal setup handoff. |
| Setup queue/tools | Setup-phase queue controls, Jay/Sybillian mode selection, setup utilities, and setup-room entry. | `setup_tools/*`, `queue/*`, `setup_room/*`, `patch_toggle/*`, `botc_patch:load`, `Jays-Patch/tool-items.json`, `tools/generate-tool-items.ps1` | During phase `0`, Toggle Jay's Patch remains in visual slot 6 (`hotbar.5`) and cycles: Jay item mode -> Jay dialog mode -> Sybillian setup bag -> Jay-held items disabled -> Jay item mode. Lime, yellow, orange, and red candles identify those states. Both Jay modes keep Reset Game, Become a Player, Jay's Setup Bag, and all setup-room controls as held items; the dialog dashboard is active-game only. The Sybillian-bag and disabled states print a bold red OP-required warning. `patch_items_enabled`, `patch_setup_bag_enabled`, and `patch_dialog_mode` explicitly define each state; migration 2 initializes only the new dialog flag so existing item/bag choices survive reloads. Non-queued players get Become the Storyteller; queued players get Queue Status and Leave Queue. Generated setup rows use mode-aware cleanup and repair checks. |
| Votekick | Public majority-vote kick flow with one active target, cooldowns, timeout cleanup, and owner immunity. | `vote/*`, `melius-commands/commands/botc.json`, `launcher/exe/BotcLauncher.cs` | Majority is strict `floor(eligible / 2) + 1`, excluding the target. Votes expire after two minutes. `function-permission-level=3` is owned by the launcher so the datapack can run `kick` only after the vote succeeds. If the target is a Storyteller, Jay's Patch removes their Storyteller state before kicking them so those powers do not persist when they rejoin. |
| Owner immunity | Configured owner list used for queue bypass and votekick target immunity. | `Jays-Patch/config/owners.txt`, `config/owners`, `tools/generate-owner-config.ps1` | Edit the text config, regenerate, deploy, and reload. Static owners are applied to `botc_owner_static` each tick and merged into `botc_owner`; owner add/remove is no longer exposed through public `/botc` commands. |
| Setup commands | Role on/off, clear, built-in preset, import, and apply bridge into Sybillian setup flow. | `setup/*`, `tools/generate-setup-role-clear.ps1` | `/setupbag` is the setup-game command surface. Setup mutation is phase-guarded to pre-game phase `0` and Storyteller-guarded before server-authority execution. High-impact preset, import, clear, and apply entrypoints route through `setup/bridge/*`, which uses `botc_setup_bridge_cd` as a short per-Storyteller debounce; role toggles stay unthrottled so normal checkbox-style setup remains responsive. Built-in presets set the same `role_list` scores as the checkbox buttons, copy preset roles onto the Script item's `script.characters` data for FancyMenu display, then call Sybillian's `set_from_menu`. Custom `/setupbag import <script-json>` calls Sybillian's full import sequence, then clears selected Jay setup-wall roles, syncs the empty selection to Sybillian setup state, and redraws the setup wall from `ct:script in_characters`; treat it as blocked if the 65,536-command limit returns. The setup-bag preset buttons close the GUI after selection so FancyMenu rereads the refreshed Script item when reopened. Role-list cleanup is generated from the known role table instead of resetting every scoreboard holder. |
| Setup room and role wall | Server-side visual setup room, replacement setup bag, base-script selector items, post-mode setup action items, role icons, role-name labels, interaction hitboxes, and Sybillian `role_list` scores. | `setup_room/*`, `setup_wall/*`, `setup/prepare_new_game_inventory`, `Jays-Patch/base-scripts.json`, `Jays-Patch/tool-items.json`, `tools/lib/sybillian-role-catalog.ps1`, `tools/generate-tool-items.ps1`, `tools/generate-setup-wall.ps1` | Base-script membership is owned once in `base-scripts.json`; IDs, display names, categories, and alignment metadata are parsed from Sybillian's installed role tables. The replacement setup bag uses Jay-owned `setup_wall_bag` model data while visually reusing Sybillian's black setup-bag model, and is only repaired when both `patch_items_enabled` and `patch_setup_bag_enabled` are `1`. If Jay's setup bag is disabled, setup-room state is cleared and Sybillian's `ct_bag`, Grimoire, and Script are repaired only when missing or moved. Storyteller clicks are accepted only during phase `0`. Opening the Setup Bag teleports the Storyteller to the room, shows a two-line colored chat guide, and clears the wall instead of assuming a default script. Setup-room bag cleanup/repair plus control/action hotbar rows are generated from `tool-items.json`; update the registry and rerun `tools/generate-tool-items.ps1` instead of hand-editing `item_checks.mcfunction`, `give_controls.mcfunction`, or `give_action_controls.mcfunction`. Script selector items clear selected roles, sync Sybillian script storage and the Storyteller Script item, redraw the wall for TB/SNV/BMR, and leave every role disabled until the Storyteller clicks individual role icons. Custom imports redraw the same wall from Sybillian `ct:script in_characters`, so custom scripts are server-side display data rather than client UI data. The Custom Script item opens a typed dialog that submits pasted JSON through guarded `/setupbag import`. The setup-wall tick only coordinates clicked interactions and throttled selected-role particles; generated click/particle dispatchers cover the full known Sybillian role list without running the full scan every tick. TB, SNV, and BMR sit in visual slots 2-4 (`hotbar.1..3`), and Custom Script sits in visual slot 5 (`hotbar.4`). Role names are generated as small text displays above the icons, blue for town/outsider and red for minion/demon. After a mode is selected, visual slots 2-4 (`hotbar.1..3`) become Clear setup, Use this bag, and Start, while visual slots 5-6 are cleared. Clear setup resets all known role scores and rebuilds Sybillian `ct:roles`; Use this bag applies the current active roles through `ct:admin/setup/set_from_menu`; Start silently applies the wall, clears wall highlight visuals, starts a new reward epoch, fully clears only the supported game's active non-Storyteller/non-spectator inventories, clears stale marked victory rockets, resets previous-game role state through Sybillian `ct:util/reset_player`, and then runs Sybillian's setup. Sybillian subsequently gives the new Notebook, seats, and roles. The Storyteller is teleported to the den, hears the bell cue after that teleport, and receives the normal `ct:start_game/roles/youare` announcement flow. A one-shot setup-wall cleanup also runs when phase leaves setup, so selected-role visuals are cleared even if the game starts through another command path. Enabled roles use lifted/scaled bright icons and dense white wall-hugging particle rings during setup phase, avoiding persistent glow outlines or display panes. The Back item returns the Storyteller to world spawn, restores their previous gamemode, and clears temporary setup-room controls. `setup_room/teleport_private` owns only the setup-room coordinate. The wall toggles Sybillian `role_list` scores through `ct:admin/setup/set_from_menu`. |
| Reset | Calls Sybillian reset behavior and returns users to normal player state without restarting the server. | `cmd/reset_game`, `reset/*` | `reset/game_state` owns the shared nomination, winner, grimoire, Sybillian-reset, and marker-repair sequence used by both normal and setup-preserving reset entrypoints. A reset-generation marker cleans players who were offline during reset when they rejoin. During a live game, non-participants whose `game_id` does not match `active_game` have stale role/team tags stripped once per active-game generation through `botc_outsider_seen`, without changing spectator/location state. `Jays-Patch/world-template` stays the shareable/manual recovery source, not the normal reset path. |
| Command overlays | Public and Melius broker command surfaces. | `melius-commands/commands/*.json` | `/st`, `/setupbag`, `/settings`, `/tpchurch`, and `/tpallhome` are guarded Storyteller/setup brokers. `/request_chat` and active-game `/character` are intentionally player-facing Sybillian compatibility surfaces; `/character` only updates the caller's FancyMenu display. `/botc` remains for Jay-owned custom features and manual compatibility fallbacks. The launcher overlays only these Jay-owned filenames and preserves unrelated upstream Melius commands. |
| Resource pack | Custom hand/reveal textures, role icon model wiring, and dialog role/UI glyphs. | `resourcepack/*`, `Jays-Patch/role-icons.json`, `Jays-Patch/dialog-icons.json`, `Jays-Patch/music-tracks.json`, `Jays-Patch/tool-items.json`, `tools/generate-role-icons.ps1`, `tools/generate-dialog-icons.ps1`, `tools/lib/role-icon-glyphs.ps1`, `tools/lib/dialog-icons.ps1`, `tools/generate-tool-items.ps1`, `tools/tests/test-resourcepack-mappings.ps1`, `tools/tests/test-dialog-ui-and-music.ps1`, `tools/tests/test-tool-item-registry.ps1` | Role art is generated from Sybillian textures into Jay-owned item textures because direct `ct:role/...` item-model references rendered as missing textures. Role paper mappings, per-role model files, copied role PNGs, and the `botc_patch:role_icons` bitmap font are generated from the shared role list. Role glyphs use stable U+E000 + Sybillian role-score mapping. General controls and music tracks use the separate generated `botc_patch:ui_icons` font with explicit U+E100-series code points, referencing Jay-owned textures or vanilla Minecraft textures directly. Ordinary text explicitly returns to `minecraft:default`, and all icon buttons retain text fallbacks for players without the optional pack. `tool-items.json` remains the registry for Jay-owned right-click tool labels, slots, phases, model-data ownership, and generated fixed-row cleanup/repair ownership. Tests catch drift between datapack strings, selector JSON, fonts, track routing, and model files. |
| Standalone launcher | Server startup, live/stop-time standard backups, Minecraft-only restart, deploy, resource-pack config, server-list branding, server-property ownership, and console UI. | `BOTC.exe`, `launcher/exe/BotcLauncher.cs`, `launcher/exe/BotcLauncher.Models.cs`, `launcher/exe/BotcLauncher.Process.cs`, `launcher/exe/BotcLauncher.Progress.cs`, `launcher/exe/BotcLauncher.ResourcePack.cs`, `launcher/branding.txt`, `launcher/compose.yml`, `launcher/local-settings.example.properties`, `data/server-icon.png`, `tools/build-botc-exe.ps1`, `tools/tests/test-startup-scripts.ps1`, `tools/tests/test-source-safety.ps1`, `tools/tests/test-source-ownership.ps1` | The launcher compiles all C# files under `launcher/exe`; data/config models, external-process/RCON execution, progress rendering, and resource-pack deployment are focused extractions from the main startup hotspot. Resource-pack URL/SHA/UUID/required state come from the canonical required-properties file. BOTC.exe writes a separate deterministic local diagnostic ZIP and compares extracted contents with the exact cached hosted fallback, so archive metadata does not create false warnings. `BOTC.exe` is the user-facing standalone entrypoint; it starts Docker Desktop when installed but not running, verifies DNS from inside the pinned Minecraft image before Modrinth installation, starts the server when offline, and opens console-only mode when online without calling runtime PowerShell launcher scripts. Its help menu only lists launcher-owned console commands; Minecraft commands are typed directly and sent to the server without local autocomplete. `backup` safely replaces the single `backups/standard` slot while Minecraft stays online; both live and stop-time backups restore `save-on` on failure and promote staging through a rollback slot. `restart` saves and restarts only Compose service `mc`, keeps Playit/Docker Desktop running, and performs Final Sync after readiness. The centered startup bar learns from recent successful durations and combines elapsed time with log-stage evidence instead of stage ceilings. The `stop` command still asks before replacing `backups/standard`; the retired `backups/latest` and `promote-backup` flow should not return. Interactive windows use the fixed dashboard; redirected/non-interactive startup deliberately skips cursor rendering and emits bounded progress rows. Console `WARN` output is reserved for actionable problems; non-error operational notes should use `NOTE` or dashboard details. Run `tools/tests/test-startup-scripts.ps1` after launcher edits, or `tools/tests/test-source-safety.ps1` during source-only stabilization. It deploys into the Docker-mounted server data folder, reads visible branding from `launcher/branding.txt`, uses Minecraft's normal `data/server-icon.png` as the single logo source, keeps `function-permission-level=3` and `spawn-protection=0` in `server.properties`, and `launcher/compose.yml` must exclude only Jay-owned command filenames from Modrinth overwrites. The launcher records those names in an ownership manifest, removes only retired Jay-owned commands, preserves unrelated upstream Melius files, and does not deploy server-side FancyMenu customizations. Final Sync remains the launcher-owned post-start deploy/reload path; hosted servers that do not run `BOTC.exe` use the tiny delayed datapack YAWP repair instead of Docker `RCON_CMDS_STARTUP`. The ownership and upstream-contract tests guard the Lego rule by keeping Jay-owned datapack behavior out of upstream `ct` namespaces and proving direct upstream calls still resolve. |
| Clean world template | Command-block-free world for sharing Jay's Patch and manual recovery. | `Jays-Patch/world-template`, `Jays-Patch/world-template-manifest.json`, `tools/update-world-template-manifest.ps1` | The binary world stays outside ordinary Git source, while its tracked SHA-256 manifest binds every release-owned world file to the source version. Public builds fail if the template changes without an intentional manifest refresh. Jay's datapack subtree is excluded because package staging replaces it from current source. |

## Script Selection Transaction

TB, SNV, BMR, and custom JSON imports all rebuild the complete Sybillian
`ct:script` state through `setup/import/commit*`. This includes role categories,
night order, reminders, `current_script`, normalized name/author metadata, and the
generated Script item. Role enablement remains separate, so choosing a setup-room
script still leaves every wall role disabled until the Storyteller selects it.

Opening or cancelling the Custom Script dialog does not mutate the current setup.
Validation failures restore every backed-up `ct:script` field and regenerate the
previous Script item before temporary import state is removed.

## Storyteller's Passage State

Passage snapshots one numeric per-player voice-zone ID when it opens. The shared
zone detector mirrors every Sybillian voice marker except Town Square and detects
the Storyteller Den directly, without waiting for Sybillian's `den` tag. Starting
inside a valid zone still requires leaving it before that same zone can close
Passage during the day. At night, Passage can only open inside a player house;
leaving that starting house immediately teleports the still-spectating
Storyteller to the den, clears stale `in_house`, and only then restores the saved
mode and hotbar. Phase changes, Storyteller-role loss, and item-disable cleanup
request a safe close through the same two-block body-space guard instead of
restoring a player inside a wall. If night ends while Passage is open, it closes
safely before `night_exit` restores the true pre-night gamemode.
