# 1.9 Beta Consistency Cleanup

- Status: complete
- Updated: 2026-08-03 20:05 +02:00
- Owner: Sol
- Workflow decision: `use Sol/Luna workflow`
- Workflow reason: The accepted cleanup crosses several source generators, generated outputs, behavior tests, public documentation, and one reveal bug; a durable integration record is worth the bookkeeping.
- `/plan` state: accepted

## Outcome

Make the 1.9 beta's visible language, documentation, and tests follow Jay's
writing guide consistently, repair the approved Draft recovery and override
paths, remove the retired general-recycling branch, and make Wraith appear in
Grimoire reveals.

## Done when

- [x] Alchemist-Summoner can be deferred, reopened, and resolved without
  stranding Draft or leaving another Storyteller with a live stale choice.
- [x] A Storyteller can explicitly confirm a duplicate private Draft override
  without enabling or restoring general recycling, and the player sees no
  pre-start change.
- [x] The dead `draft_recycling` picker branch is gone while the accepted
  all-type exact-shortfall completion recycler remains covered.
- [x] A Wraith saved as role `325` produces the normal Wraith character display
  during Grimoire reveal.
- [x] Draft, Grimoire editor, Boomdandy, ordinary errors, public documentation,
  and internal current-state documentation use the accepted visible language.
- [x] Owning generators are regenerated, focused checks and the complete
  source gate pass, and unrelated dirty work remains preserved.

## Scope and boundaries

- In scope: Draft generator and tests, Grimoire editor/reveal source and tests,
  Boomdandy generator and tests, player-facing style checks, current feature
  documentation, the internal README, and the public-package README.
- Non-goals: changing accepted Draft probability/topology rules, changing
  Boomdandy execution behavior, changing Storyteller final authority, changing
  Wraith gameplay, editing Sybillian `ct:` source, or rebuilding a public
  release package.
- Must preserve: exact-shortfall completion recycling for every still-required
  type, official jinx wording, cinematic exceptions, hidden-character privacy,
  unrelated Greedy/live-data/package work, and all existing user changes.
- Safety constraints: edit `Jays-Patch` source owners before generated output;
  do not restart the server; deploy/reload and a bounded Wraith reveal fixture
  are now explicitly authorized by Jay. Stop before mutation if an unrelated
  player or active game is present, and restore the pre-test setup state.

## Evidence

- Git is on `codex/jays-patch-1.9-beta-recovery` with the expected 615-entry
  dirty baseline before this task's edits.
- Jay accepted the complete proposed cleanup and added the Wraith reveal bug.
- `grim/editor/refresh_live_roles` captures Wraith role `325`, and the trusted
  editor score catalog contains Wraith.
- `grim/reveal/from_snapshot` copies the saved role to `grim_reveal_role`, but
  `grim/reveal/spawn_from_score` maps only scores `1..137`; role `325` matches
  no reveal spawn command.
- `draft_recycling` has no enabling control or setter. A separate
  `completion/*` state machine owns the accepted exact-shortfall behavior.

## Inference

- Wraith disappears only at the final reveal spawn dispatch; the snapshot,
  alignment, icon, model, and translation paths do not need a parallel patch.

## Unknowns

- None that change the accepted implementation plan. New contradictory
  evidence will stop only the affected slice for review.

## Recommendations

- Keep the Wraith fix in the shared role-score reveal dispatch and extend the
  existing Wraith regression suite so future role extensions cannot repeat the
  omission silently.
- Keep visible-copy tests targeted at rendered messages and explicit retired
  phrases; do not treat internal comments or structured red detail rows as
  ordinary standalone errors.

## Project-owner decisions

| Decision | Reason | Date |
|---|---|---|
| Implement the complete proposed 1.9 consistency cleanup | Jay approved the behavior, wording, documentation, and verification direction as written. | 2026-08-03 |
| Wraith must appear during Grimoire reveal | Jay observed that it currently does not show when revealed. | 2026-08-03 |
| Deploy and run the Wraith reveal check now | Jay explicitly accepted the recommended intentional deployment and in-game Wraith verification. | 2026-08-03 |

## Accepted `/plan`

1. [x] Repair Alchemist-Summoner recovery, duplicate override confirmation, and
   retired general-recycling code with focused Draft tests.
2. [x] Add Wraith to the shared reveal dispatch and extend Wraith/Grimoire
   regression coverage.
3. [x] Apply the accepted Draft, Grimoire, Boomdandy, and shared-error language
   through their source owners, then regenerate output.
4. [x] Reconcile the design journal, completed task journals, feature map,
   internal README, and public README without deleting historical evidence.
5. [x] Run focused generators/tests, the complete source gate, whitespace and
   final-diff review without touching the live server.
6. [x] Deploy the reviewed Jay-owned runtime, reload without restarting, and
   verify a Wraith snapshot reveal plus complete fixture cleanup in game.

## Delivery tracking

- Decision: use accepted-plan checklist
- Reason: The accepted plan plus the host checklist is durable enough; no
  separate `/goal` was requested or needed.

### Active `/goal` (only when used)

Not used; accepted plan is the delivery checklist.

## Current progress

- Re-read the applicable workflow template and prior message-consistency
  journal after resumption.
- Verified branch and dirty baseline.
- Proved the Wraith reveal omission at the final score-to-spawn dispatcher.
- Made the complete Grimoire reveal score dispatcher generator-owned; it now
  covers all 138 trusted characters, including Wraith score `325`.
- Removed the dead general Draft recycler, added deferred Alchemist-Summoner
  recovery, and added explicit `Use Anyway` confirmation for duplicate trusted
  overrides without changing player-facing pre-start state.
- Rewrote Draft, Grimoire editor, and Boomdandy visible copy through their
  generators and standardized ordinary bare-red errors through the affected
  Storyteller tool source owners.
- Strengthened the style regression to parse quoted and unquoted first
  components, preserve intentional cinematic/structured exceptions, and reject
  exact retired visible phrases.
- Reconciled active README, public README, feature map, and historical task and
  design journals with the accepted Draft and Boomdandy decisions.
- Live preflight found the healthy `botc-minecraft` container in setup phase
  with Buffet and Grimoire inactive. Jay plus five named test players are
  online; no unrelated player is present.
- Pre-deployment runtime parity correctly failed because the live pack lacked
  two newly generated functions. The reviewed source datapack was copied into
  place through a recoverable swap and runtime parity then passed.
- The first reload exposed a deployment-fixture mistake: the recoverable old
  pack was still inside `world/datapacks`, so Minecraft enabled both copies and
  the stale dispatcher won the duplicate function path. Direct Wraith spawn
  worked, which isolated the failure to pack precedence rather than the icon,
  model, translation, or spawn function.
- Moved the preserved backup to `data/deploy-backups`, outside datapack
  discovery, and reloaded. Minecraft now reports exactly one enabled Jay pack.
- A bounded seat-1 snapshot with Wraith role `325` exercised
  `grim/reveal/from_snapshot`. The live item display reported
  `botc_role_wraith`, the Wraith translation, and the expected evil reveal
  text. Cleanup removed the reveal entities and spotlight and restored setup
  phase with Buffet and Grimoire inactive.

## Active Luna assignments

| Assignment | Worker journal | Allowed area | State |
|---|---|---|---|
| None | N/A | Sol owns the overlapping generator and documentation integration. | N/A |

## Verification record

| Check | Result | Evidence |
|---|---|---|
| Branch and dirty baseline | pass | Correct branch; 615 pre-task status entries. |
| Wraith reveal trace | fail before fix | Role `325` reaches `grim_reveal_role`, but `spawn_from_score` ends at role `137`. |
| Draft, Grimoire, Wraith, and Boomdandy focused suites | pass | Buffet gamemode, deterministic Draft model, Grimoire editor, Wraith, and Boomdandy/nomination checks passed after regeneration. |
| Player-facing style check | pass | Broad first-component parsing found only the two intentional allowlisted cinematic red messages; retired visible wording is absent. |
| Git whitespace check | pass | `git diff --check` passed after behavior and wording regeneration. |
| Generated output and index checks | pass | Draft, Greedy, and Grimoire generator checks passed; generated code-library indexes are current. |
| Pre-baseline complete source gate | pass | Every non-live source-only suite passed with only the stale baseline and historical public-package comparison skipped. |
| Mandatory baseline safety rerun | pass | The updater reran the complete source safety suite and refreshed the known-good baseline for 3,024 owned files. |
| Final baseline-enabled source gate | pass | Every non-live source-only suite passed with baseline validation enabled; only the historical public-package comparison and live mutation test were intentionally excluded. |
| Final diff review | pass | The generated reveal dispatcher includes scores `1..137` plus Wraith `325`; retired visible phrases are absent, only two intentional cinematic red-first messages remain, and unrelated dirty work is preserved. |
| Pre-deployment runtime parity | expected fail | The live pack was stale and lacked `draft/review/editor/confirm_duplicate` and `draft/topology/alchemist_summoner/show`. |
| Post-deployment runtime parity | pass | Jay's Patch datapack and owned Melius command files match source byte for byte before and after reload. |
| Enabled datapack audit | pass | Only `file/jays_patch` is enabled; the recoverable old copy is stored outside datapack discovery. |
| Live Wraith snapshot reveal | pass | Seat 1 produced one reveal item with `botc_role_wraith`, `clocktower.role.wraith.name`, evil reveal text, and a revealed snapshot score of `1`. |
| Live fixture cleanup | pass | No reveal entity or spotlight remains; phase, Buffet, and Grimoire all returned to `0`. |
| Reload and post-test health | pass | Container remains `running healthy`; no Jay parser/function/command errors appeared. Two known unrelated FancyMenu `spiffy_structures` codec errors remain. |

## Current blocker

None.

## Exact next step

None. Preserve the external runtime backup until Jay no longer needs rollback.

## Final outcome

Source implementation, authorized live deployment, and Wraith reveal
verification are complete. The runtime matches source, the server is healthy,
the test fixture was fully removed, and historical public packages remain
untouched.
